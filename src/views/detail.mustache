<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link rel="stylesheet" href="http://localhost:3333/styles/global.css">
  <link rel="stylesheet" href="http://localhost:3333/styles/detail.css">

  <title>Home</title>
</head>
<body>
  <header>
    <h1>{{new.title}}</h1>
  </header>

  <div id="content">
    <div class="new">
      <h3>{{new.title}}</h3>
      <div class="detail">
        <h5>Description: </h5>
        <span>{{new.description}}</span>
      </div>
      <div class="detail">
        <h5>Content: </h5>
        <span>{{new.content}}</span>
      </div>
      <span class="author">{{new.author}}</span>
      <span class="view_more"><a href="/">Back</a></span>
    </div>
  </div>
</body>
</html>